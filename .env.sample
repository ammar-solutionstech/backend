# Server
PORT=8080

# JWT
JWT_SECRET=JWT_secret
JWT_EXP_MINUTES=15
JWT_ISSUER=JWT

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=db_user
DB_PASSWORD=db_passwd
DB_NAME=db_name
DB_SCHEMA=db_schema
SSL_MODE=disable
